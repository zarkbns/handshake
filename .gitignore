cache/
out/
session-ses_*.md
node_modules/
lib/
broadcast/
.env
.env.*
!.env.example
.vscode
.agents
.env.local
# Stray literal "~/" directory created by an unquoted tilde in a past command.
# It holds local tool configs with live API keys and must never enter history.
/~/
# Local audit/agent scratch bundles.
.audit-*/

